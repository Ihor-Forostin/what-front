pipeline {
  agent any
 
  tools {nodejs "node"}
 
  stages {
   
    stage('Build') {
      steps {
        sh 'npm config ls'
        sh 'npm install'
        sh 'npm audit fix --force'
        sh 'npm run build'
      }
    }
  }
}
