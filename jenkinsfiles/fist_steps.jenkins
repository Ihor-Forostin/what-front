pipeline {
  agent any
 
  tools {nodejs "node"}
 
  stages {
    stage('Example') {
      steps {
        sh 'npm config ls'
        sh 'npm install'
        sh 'npm audit fix --force'
        sh 'apk add g++ make python'
        sh 'npm run build'
      }
    }
  }
}
